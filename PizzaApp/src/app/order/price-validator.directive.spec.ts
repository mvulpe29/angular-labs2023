import { PriceValidatorDirective } from './price-validator.directive';

describe('PriceValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new PriceValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
